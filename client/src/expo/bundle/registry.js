
define([], function() {

	/**
	 * Bundle Registry where all bunlde resources are stored
	 * @alias bundle/registry
	 */
	var BundleRegistry = {

	};

	// Return the bundle registry object
	return BundleRegistry;

});